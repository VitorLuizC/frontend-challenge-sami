{
  "private": true,
  "name": "frontend-challenge-sami",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check \"src/**/*.tsx\"",
    "lint:fix": "prettier --write \"src/**/*.tsx\""
  },
  "devDependencies": {
    "@types/prettier": "^2.0.1",
    "prettier": "^2.0.5"
  }
}
